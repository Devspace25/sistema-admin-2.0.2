# Configuración de conexión a base de datos (servidor)
# Recomendado: PostgreSQL
# Formato: postgresql+psycopg2://usuario:password@IP_SERVIDOR:5432/nombre_bd
DATABASE_URL=postgresql+psycopg2://admin_app:StrongPass123@192.168.1.10:5432/admin_app

# Carpeta de datos local (solo aplica para SQLite o archivos como recibos)
#ADMIN_APP_DATA_DIR=C:\\Users\\%USERNAME%\\AppData\\Roaming\\Sistema-Admin-2.0\\data

# Evita que la app llame a Base.metadata.create_all() automáticamente al crear
# la sesión. Útil cuando el esquema lo gestiona el servidor o se usan migraciones.
# Valores aceptados: 1, true, yes (case-insensitive)
#ADMIN_APP_SKIP_CREATE_ALL=1
